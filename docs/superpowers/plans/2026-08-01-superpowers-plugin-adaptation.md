# superpowers 插件技能适配计划（分级替换冲突处）

> **面向 AI 代理的工作者：** 由主 Agent 按步骤执行；步骤使用复选框（`- [ ]`）跟踪进度。本计划全部产物为 Markdown 技能文件，无代码测试步骤。

**目标：** 把 superpowers-zh 插件 6 个技能按 my_harness 工作流适配——**只修改与工作流冲突的章节，替换为分级（档位）机制，原版其余内容全部保留**。

**架构：** 以"保留原版 + 冲突处分级替换"为原则。从版本库基线恢复 6 个原版 SKILL.md，对每个技能做最小外科式修改：在冲突章节插入或替换分级规则，非冲突内容零改动。适配结果快照回版本库。

**技术栈：** Markdown skills（SKILL.md），无代码。

---

## 适配原则（本次与上次的区别）

- 上次：整体重写为精简裁剪版（删除了大部分原版内容）→ **错误**。
- 本次：只替换冲突章节为分级逻辑，原版内容（流程、图表、红线、常见错误、引用文件）原样保留。
- 验收标准：**非冲突内容零删减**——每个技能 diff 基线后，除声明过的冲突处与新增的分级门禁章节外，无任何其他改动。

## 冲突点与分级替换方案

### 1. using-superpowers

**保留：** 技能优先级、红线表、平台适配、中国特色技能路由、用户指令、SUBAGENT-STOP。

**替换/新增：**
- 替换 `<EXTREMELY-IMPORTANT>` 的"1% 必须调用"规则 → 档位原则：先读 `~/.codex/rules/workflow-policy.md` 定档位，命中档位裁剪表才调用。
- 替换"规则"第 1 条（任何响应/澄清前先调用技能）→ 先定档位再按表调用；澄清与探索不机械消耗技能调用。
- 替换"进入 EnterPlanMode 之前必须先头脑风暴" → 按档位：轻量压缩、中量关键设计确认、重量完整。
- 新增"档位裁剪表"章节（brainstorming / TDD / worktrees / DPA / SDD 等技能的轻/中/重允许档位）。

### 2. brainstorming

**保留：** 概述、反模式、流程图、流程详述、核心原则、视觉伴侣。

**替换/新增：**
- 替换 `<HARD-GATE>` → 分级门禁：轻量级压缩为必要澄清+简短方案确认（不写 spec）；中量级合并澄清+关键设计确认（spec 可选）；重量级完整流程。门不变：**未经用户确认设计不得进入实现**。
- 检查清单第 3 条（每次一个问题）→ 标注"档位裁剪时可合并提问"。
- 检查清单第 6/8/9 条（写 spec / 用户审查规格 / 调 writing-plans）→ 标注"轻量级跳过，中量级可选"。
- "设计之后"章节 → 标注轻量级不适用 spec 与 writing-plans。

### 3. test-driven-development

**保留：** 概述、铁律、红-绿-重构全流程、好的测试、为什么顺序很重要、红线、`testing-anti-patterns.md`。

**替换/新增：**
- 替换"何时使用"（始终使用/例外）→ 分级：轻量级不强制 TDD（唯一测试轮给最高信息量验证）；中量级高风险逻辑优先；重量级完整 TDD；原"例外"清单保留。
- 铁律加注：仅在实际执行 TDD 的档位/场景生效。
- 新增"测试配额口径"（轻量 1 轮 / 中量 3 轮；修复后重跑计新轮；用户显式要求完整 TDD 时放宽必需轮次并声明）。

### 4. using-git-worktrees

**保留：** 步骤 0 检测逻辑、submodule 守卫、原生工具优先、git 回退细节、目录选择、安全验证、项目设置、基线验证、快速参考、常见错误、红线、集成。

**替换/新增：**
- 概述后新增"门禁（默认禁止）"：`git-development.md` 规定未经允许不准创建 worktree/branch/tag；使用前须 HITL Checkpoint 授权；未授权原地工作；已有原生隔离时不创建。
- 替换步骤 0 中"创建 worktree 之前先征求同意"→ 直接引用门禁（默认禁止，已获 HITL 授权才继续）。

### 5. dispatching-parallel-agents

**保留：** 概述、何时使用、模式（识别问题域/创建任务/并行派发/审查集成）、智能体提示词结构及后续全部内容。

**替换/新增：**
- 概述后新增"Harness 门禁"：派发前读 `workflow-policy.md` + `multi-agent-assistance.md`；档位配额（轻量可读写 1/只读 3，中量 3/5，重量不额外限制）；身份冻结；子 Agent 不继续派发；禁止并行清单与独立条件（引用 workflow-policy）。

### 6. subagent-driven-development

**保留：** 概述、何时使用、与 executing-plans 对比、流程、起飞前计划审查、模型选择、处理实现者状态、提示词与脚本。

**替换/新增：**
- 概述后新增"Harness 门禁"：档位配额；**串行派发**（严禁同时派发多个实施 Agent）；并行写入改用 `dispatching-parallel-agents`；子 Agent 约束；Git 约束（允许范围明确的普通本地提交，不 amend、不 push）。

## 文件结构

- 修改（插件，6 个文件）：`C:\Users\wjf\.codex\plugins\local\superpowers-zh\skills\<name>\SKILL.md`
- 版本库：`C:\Users\wjf\.agents\skills\superpowers`（基线 `dede6f3`；错误快照 `5bb94ad` 将被重置，替换为适配版）
- 项目：`docs/superpowers/plans/2026-08-01-superpowers-plugin-adaptation.md`（本计划）；旧计划 `2026-07-31-...` 保留为历史记录

## 任务

### 任务 1：恢复原版并重置版本库错误快照

- [ ] 从版本库基线 `dede6f3` 复制 6 个原版 `SKILL.md` 回插件对应目录
- [ ] 重置版本库 `5bb94ad`（错误裁剪快照）→ `git reset --hard dede6f3`，使基线保持唯一原版记录
- [ ] 校验：插件 6 个技能与基线一致（hash 对比）

### 任务 2：适配 using-superpowers

- [ ] 按上文方案 1 替换冲突章节、新增档位裁剪表；其余内容原样保留
- [ ] 校验：diff 基线仅含声明过的冲突处与新增章节

### 任务 3：适配 brainstorming

- [ ] 按方案 2 替换 HARD-GATE、标注检查清单与"设计之后"；其余保留
- [ ] 校验同上

### 任务 4：适配 test-driven-development

- [ ] 按方案 3 替换"何时使用"、铁律加注、新增配额口径；其余保留
- [ ] 校验同上

### 任务 5：适配 using-git-worktrees

- [ ] 按方案 4 新增门禁、替换步骤 0 征求同意段；其余保留
- [ ] 校验同上

### 任务 6：适配 dispatching-parallel-agents

- [ ] 按方案 5 新增 Harness 门禁；其余保留
- [ ] 校验同上

### 任务 7：适配 subagent-driven-development

- [ ] 按方案 6 新增 Harness 门禁；其余保留
- [ ] 校验同上

### 任务 8：快照适配版到版本库

- [ ] 复制 6 个适配后 `SKILL.md` 回版本库
- [ ] 更新 README 版本记录；`git add -A && git commit`（如 `feat: 按 my_harness 分级适配 6 个技能`）

### 任务 9：整体验证与收尾

- [ ] 逐个技能 diff 基线，确认非冲突内容零改动（逐行核对声明过的冲突处）
- [ ] frontmatter 合法、无占位符；分级措辞与 workflow-policy 一致
- [ ] 项目仓库提交计划文档与文档更新（如 `docs(流程): 插件技能分级适配计划`）
- [ ] 汇报结果；新对话验证技能列表与行为

## 风险

- 原版内容较长（如 TDD/SDD 数百行），修改时需逐行 diff 防止误删 → 用版本库基线 diff 兜底。
- 插件仓库自身是 git 仓库：适配后 6 个文件显示为 modified（不提交，留给插件自身管理）；原版随时可从版本库恢复。